nelmio_api_doc:
  documentation:
    info:
      title: 'Sport Event Manager API'
      description: 'API for managing sports events'
      version: '1.0.0'
    components:
      securitySchemes:
        Bearer:
          type: http
          scheme: bearer
          bearerFormat: JWT
    security:
      - Bearer: []
  areas:
    default:
      path_patterns: ['^/api']
